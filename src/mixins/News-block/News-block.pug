mixin News-block(param={})
  .news-block
    ._title 
      span.active Новости
      span /
      span Акции
    a(href="#")._subtitle=param.titleLink 
    .news-block-tab.active
      +News-card({
      })
      +News-card({
      })
      +News-card({
      })
      +News-card({
      })